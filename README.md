# PrintNet — Express + TypeScript starter

Quick commands:

- Install dependencies (runtime + dev):

  npm install express
  npm install -D typescript ts-node-dev @types/express @types/node

- Development (auto-restart):

  npm run dev

- Build:

  npm run build

- Run built app:

  npm start
